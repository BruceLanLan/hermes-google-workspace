"""Google Workspace skill for AI agents (Hermes, OpenClaw, Claude Code, ...).

Installed as a Python package named ``gws_cli`` (see pyproject.toml), but the
source layout lives under ``scripts/`` so the files can also be run directly
as standalone scripts or invoked via the ``bin/gws*.cjs`` wrappers.
"""

__version__ = "2.2.0"
